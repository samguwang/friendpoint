# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'friendpoint' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'GoogleMaps'
  pod 'GooglePlaces'

  # Pods for friendpoint

  target 'friendpointTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'friendpointUITests' do
    # Pods for testing
  end

end
